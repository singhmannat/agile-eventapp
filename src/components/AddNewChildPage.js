import React from "react";
import ParentNavHeader from "./ParentNavHeader";

export default class AddNewChildPage extends React.Component {
  render() {
    return (
      <div>
        <ParentNavHeader />
        <p>Add a new Child page</p>
      </div>
    );
  }
}
